{
  "devDependencies": {
    "typescript": "^4.9.4"
  },
  "name": "meta-pool-ssv-bots",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node dist/index.js",
    "start:balance:bot": "npm run build && node dist/bots/balancesBot/index.js",
    "start:activatedValidators:bot": "npm run build && node dist/bots/createValidatorAlert/activatedValidators.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "alchemy-sdk": "^2.4.3",
    "ethers": "^6.0.3"
  }
}
